# File Triage App

Universal file triage tool — point it at any folder and swipe through files to **keep**, **delete**, or **move** them. Supports images, documents, text, code, video, and audio.

## Run

```bash
npm install && npm start   # then open http://localhost:3002
```
